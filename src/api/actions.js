export const NAMESPACE = 'RECAST';

const playlist = {
  request: `${NAMESPACE}_PLAYLIST_REQUEST`,
  requestSuccess: `${NAMESPACE}_PLAYLIST_REQUEST_SUCCESS`,
  requestFailure: `${NAMESPACE}_PLAYLIST_REQUEST_FAILURE`
};

const actions = {
  playlist
};


export default actions;
